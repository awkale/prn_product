<div class="row">
  <div class="col-sm-6">
  <h2>Products <%= link_to 'New Product', new_product_path, { :class => "btn btn-sm btn-link"}  %></h2>
    <ol>
    <% @products.each do |product| %>
      <li><%= link_to product.product_name, product %> / <%= link_to 'edit', edit_product_path(product) %> </li>
    <% end %>
    </ol>

    <p></p>
  </div>
</div>
