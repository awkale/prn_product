<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @related_products.each do |rp| %>
    <tr>
      <td><%= link_to rp.product_name, product_path(rp) %></td>
      <td><%= rp.code %></td>
      <td><%= rp.base_price  %></td>
      <td><%= rp.length_price %></td>
      <td>
        <%= link_to 'Edit',
        edit_recipient_path(rp)
        %>
        </td>
    </tr>
    <% end %>
  </tbody>
</table>