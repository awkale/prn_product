<div class="row">
  <div class="col-sm-6">
    <h2><%= @channel.channel_name %> <% if current_user.admin %><%= link_to 'Edit Channel', edit_channel_path, {:class => "btn btn-sm btn-link"} %><% end %></h2>
    <ul>
    <% @channel.media_points.each do |c_p| %>
      <li><%= link_to c_p.media_point_name, media_point_path(c_p) %></li>
    <% end %>
    </ul>
  </div>
</div>
<hr>
<div class="row">
  <div class="col-sm-2 col-sm-offset-10">
    <%= link_to 'Delete Channel', channel_path, method: :delete, data: { confirm: "Are you sure? This can not be undone."}, :class => "btn btn-danger btn-sm pull-right" %>
  </div>
</div>