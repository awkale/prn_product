<div class="row subject new">
  <div class="col-sm-8">
    <h2 class="section-header">New subject</h2>
    <%= simple_form_for @subject do |cat| %>
      <%= cat.error_notification %>
      <%= cat.input :subject_name, placeholder: 'subject' %>
      <%= cat.button :submit %>
      <%= link_to "Cancel", :back, {:class => "btn btn-default pull-right mr-sm"} %>
    <% end %>
  </div>
</div>