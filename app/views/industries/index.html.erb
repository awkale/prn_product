<div class="row industries ">
  <div class="col-sm-6">
    <h2 class="section-header">
    Industries
      <%= link_to 'Manage Industries', manage_industries_path, :class => "btn btn-sm btn-link pull-right" if policy(Industry).manage? %>
    </h2>
    <%= sorted_nested_ul(@industries, :industry_name) do |ind| %>
      <%= link_to ind.industry_name, ind %>
    <% end %>
  </div>
</div>
